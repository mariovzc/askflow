<% commentable.comments.each do |comment| %>
<div class="jumbotron">
  <div class="row">
    <div class="col-sm-2">
      <div class="vote-container"> Arriba voto Abajo</div>      
    </div>
    <div class="col-sm-10">
      <div class="lead"><%=markdown(comment.body)%></div>                    
    </div>
  </div>
  <hr class="my-4">
  <div class="data">
    <span class="user">Preguntó: <strong><%= comment.user.email %></strong></span>
    |
    <span class="date">preguntado: <%= time_ago_in_words(comment.created_at) %></span>        
  </div>
</div>
<% end %>